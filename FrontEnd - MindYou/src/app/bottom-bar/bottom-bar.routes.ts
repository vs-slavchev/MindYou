import { Routes } from '@angular/router';
// app
import { BottomBarComponent } from './bottom-bar/bottom-bar.component';

export const BottomBarRoutes: Routes = [
    {
        path: '',
        component: BottomBarComponent
    }
];
