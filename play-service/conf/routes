
GET         /user/:id                       controllers.AppUserController.showUser(id: Long)

# in body provide as json activity_id and user_id; ex: {"id":123,"name":"Andrea"}
POST        /user/create                    controllers.AppUserController.createAppUser


# in body provide as json activity_id and user_id; ex: {"activity_id":123,"user_id":321}
POST        /activities/start               controllers.TrackedActivityController.startActivity

# as param give the id of the user; example: /activities/123/stop
GET         /activities/:userId/stop        controllers.TrackedActivityController.stopActivity(userId: String)


# when app is started some activity blueprints are queried to be shown on activities screen
# example: /activities/top?number=10 to only show the top 10 activities
GET         /activities/top                 controllers.ActivityBlueprintController.listActivities(number: Long)

#generate a friendship with two IDs of the inviter and the invitee in Json body, ex: {"inviter_id":123,"invitee_id":321}
POST        /user/friendship/create  controllers.FriendshipController.sendFriendRequest

#accept the friendship request between the inviter and the invitee,
PUT         /user/friendship/accept  controllers.FriendshipController.acceptFriendRequest(inviter_id: Long, invitee_id: Long)

# create a custom activity
POST        /activities/create              controllers.ActivityBlueprintController.createActivityBlueprint

#TODO: get user, get all users, get all friends, get all friend requests to me